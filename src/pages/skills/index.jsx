import React from "react";
import stylesheet from "@assets/css/main.module.scss";

const Index = () => {
    return (
        <section id="skills" className={stylesheet.skills}>
            <h2>Skills</h2>
            <p>I specialize in React, SCSS, and modern web technologies.</p>
        </section>
    );
};

export default Index;
